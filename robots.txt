# elmroots.com
#
# The app data under /apps/ratings-now/lists/ is machine-readable JSON with no
# search value — it exists for the Ratings Now app, which fetches it directly.
# Keeping crawlers out of it saves bandwidth and keeps analytics readable.
#
# NOTE ON DUPLICATION: Cloudflare's robots.txt management ("Instruct AI bots to
# not scrape content") injects its own AI-crawler block, wrapped in BEGIN/END
# Cloudflare Managed content markers. The AI-crawler rules below duplicate it on
# purpose, so this file still stands alone if that setting is ever turned off or
# changes behavior. Duplicate groups for the same user-agent are merged by
# conforming parsers, so the overlap is harmless — and Cloudflare's copy stays
# current as new crawlers appear, which is why that setting is worth leaving on.

User-agent: *
Disallow: /apps/ratings-now/lists/

# ── SEO/marketing crawlers with no reason to read this site ────────────────
User-agent: AhrefsBot
Disallow: /

User-agent: SemrushBot
Disallow: /

User-agent: MJ12bot
Disallow: /

User-agent: DotBot
Disallow: /

User-agent: PetalBot
Disallow: /

User-agent: DataForSeoBot
Disallow: /

# ── AI crawlers (mirrors Cloudflare's managed block; see note above) ───────
User-agent: Amazonbot
Disallow: /

User-agent: Applebot-Extended
Disallow: /

User-agent: Bytespider
Disallow: /

User-agent: CCBot
Disallow: /

User-agent: ClaudeBot
Disallow: /

User-agent: Google-Extended
Disallow: /

User-agent: GPTBot
Disallow: /

User-agent: meta-externalagent
Disallow: /
